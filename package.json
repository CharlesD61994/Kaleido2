{
  "name": "kaleido-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync:storefront": "node scripts/sync-storefront.mjs",
    "predev": "npm run sync:storefront",
    "dev": "vite",
    "prebuild": "npm run sync:storefront",
    "build": "vite build",
    "cap:sync:ios": "npm run build && node scripts/prepare-capacitor-dist.mjs && cap sync ios",
    "cap:open:ios": "cap open ios",
    "smoke": "node scripts/smoke-check.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1",
    "@supabase/supabase-js": "^2.49.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "vite-plugin-pwa": "^0.17.0"
  }
}
